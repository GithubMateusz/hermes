name: Tests

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  tests:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Install setuptools
        run: pip install --upgrade pip setuptools wheel
      - name: Install and configure Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.4.1
          virtualenvs-create: false
      - name: Install dependencies
        run: poetry install
      - name: Poetry check
        run: poetry check
      - name: Ruff
        run: |
          ruff check ./hermes
          ruff format ./hermes --check
      - name: Mypy
        run: mypy ./hermes
      - name: Bandit
        run: bandit --skip B101 -r ./hermes
      - name: Pytest
        run: pytest -v --cov=./hermes --cov-report term-missing --cov-report xml:/tmp/coverage.xml --junitxml=/tmp/pytest.xml ./tests
      - name: Pytest Coverage
        uses: MishaKav/pytest-coverage-comment@main
        with:
          pytest-xml-coverage-path: /tmp/coverage.xml
          junitxml-path: /tmp/pytest.xml
